{#
This file provides the HTML structure for baic pages.
#}

<section{{ content_attributes }}>
  {{ content }}

  {% if link %}
    <a href="{{ link.getUrl() }}" class="link-button link-button-single link-button-green">
      <span>{{ link.title }}</span>
    </a>
  {% endif %}
</section>
